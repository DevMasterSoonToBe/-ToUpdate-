@model IEnumerable<AlausDaryklosGamybosValdymoSistema.Models.Pareigybe>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Paeigybe)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Paeigybe)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PareigybesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PareigybesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PareigybesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
